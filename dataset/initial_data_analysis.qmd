---
title: "Initial Data Analysis"
editor: visual
---

```{r}
suppressPackageStartupMessages(library(tidyverse))

load("dataset/mmr.RData")

mmr_data_clean |> 
  ggplot(aes(x = val)) + 
    geom_histogram()


# this isnt working idk 
# mmr_data_clean |> 
#   group_by(location_name, year_id) |> 
#   summarize(mean = mean(val)) |> 
#   ungroup() |> 
#   ggplot(aes(x = mmr_data_clean$year_id, y = mean)) +
#   geom_point(aes(alpha = 0.001)) 


```